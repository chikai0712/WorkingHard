---
description: "GSD (Get Shit Done) 規格驅動開發工作流程 — 強制 AI 先規劃再執行，防止 Context Rot 與邏輯斷裂"
alwaysApply: true
---

# GSD 工作流程規則（全局強制）

## 核心原則：先規劃，再執行

**在任何程式碼修改之前，你必須：**

1. 確認 `CONTEXT.md` 已存在且已讀取（了解專案架構與限制）
2. 確認 `ROADMAP.md` 已存在且已讀取（了解當前階段）
3. 確認 `STATE.md` 已存在且已讀取（了解上次的進度存檔點）

如果這三個文件不存在，**停止執行，先建立它們**。

---

## GSD 五階段流程

### Phase 0：詢問 (Questioning)
- 收到任務後，**不得立即寫程式**
- 至少確認以下問題：
  - 這個功能的目的是什麼？
  - 有哪些技術限制或禁止使用的 library？
  - 完成的定義（Definition of Done）是什麼？
- 詢問完畢後更新 `CONTEXT.md`

### Phase 1：研究 (Research)
- 掃描相關程式碼，理解現有架構
- 識別潛在的衝突點
- 不做任何修改，只讀取與分析

### Phase 2：規格 (Spec)
- 生成或更新 `ROADMAP.md`
- 將任務拆解為原子步驟（每步驟應可獨立驗證）
- 每個步驟必須包含：
  - [ ] 步驟描述
  - 驗證方式（如何確認完成）
  - 相關文件路徑

### Phase 3：執行 (Execute)
- **一次只執行一個步驟**
- 執行完畢後必須自我驗證（跑測試、檢查 lint、確認功能）
- 更新 `STATE.md`，標記該步驟為完成

### Phase 4：驗證 (Verify)
- 每個步驟完成後：
  1. 執行相關測試（如 `npm test`、`pytest`、`go test ./...`）
  2. 確認沒有引入新的 lint 錯誤
  3. 在 `STATE.md` 打勾並記錄時間戳

---

## 防止 Context Rot 的規則

- 每次對話開始時，**主動讀取** `STATE.md` 並告訴使用者目前進度
- 如果對話太長（超過 20 輪），主動提示使用者開新對話並從 `STATE.md` 恢復
- **禁止在單一步驟中修改超過 3 個不相關的檔案**
- 完成一個步驟後，必須等待使用者確認再繼續下一步

---

## 緊急煞車規則

如果在執行過程中發現：
- 需要修改的範圍遠超過預期
- 遇到未知的架構限制
- 可能破壞現有功能

**立即停止，回到 Phase 2，重新更新 ROADMAP.md**，而不是硬撐繼續執行。
