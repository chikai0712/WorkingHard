//
// Primary DNS Configuration
// This DNS is intentionally disabled to simulate complete failure
//

options {
    directory "/var/lib/bind";
    pid-file "/var/run/named/named.pid";
    
    // DNS service is disabled for simulation
    // In real scenario, this would be a working Cloudflare-like DNS
};

