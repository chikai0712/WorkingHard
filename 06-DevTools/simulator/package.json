{
  "name": "creepjs-simulator",
  "version": "1.0.0",
  "description": "Simulator for generating CreepJS fingerprint data",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "node index.js",
    "generate": "node index.js --generate",
    "batch": "node index.js --batch",
    "test": "node test.js"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0"
  }
}

